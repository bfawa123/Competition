# 部署检查清单

## 赛前准备

### ✅ 数据准备
- [ ] 书目数据已填充到 60-80 本
- [ ] 书目覆盖 5-6 个主题（Python/机器学习/深度学习/NLP/计算机视觉等）
- [ ] 每本书的元数据完整（难度、页数、语言、关键词等）
- [ ] 准备 5-10 个演示用户场景

### ✅ 环境配置
- [ ] `.env` 文件已创建并配置 API 密钥
- [ ] 使用稳定的 API 服务商（或国内代理）
- [ ] `requirements.txt` 已安装
- [ ] `test_backend.py` 全部测试通过

### ✅ 功能验证
- [ ] 推荐接口能返回结果
- [ ] 记忆可以写入和读取
- [ ] 前后对比接口正常
- [ ] Agent轨迹记录完整
- [ ] 评分公式执行正确

### ✅ 性能优化
- [ ] Embedding 模型已缓存
- [ ] 推荐响应时间 < 3 秒
- [ ] 记忆检索响应时间 < 500ms
- [ ] 并发测试通过（模拟5-10个用户）

### ✅ 文档和展示
- [ ] API 文档完整（`API.md`）
- [ ] 准备评委问答FAQ
- [ ] 准备3-5分钟演示脚本
- [ ] 准备"前后对比"截图

---

## 现场调试

### 常见问题应对

#### 问题1: LLM API 调用失败
**预案**：
- 切换到备用 API 服务商
- 或使用本地模型（降低速度但保证功能）
- 或准备缓存结果，减少API调用

#### 问题2: 网络问题导致服务慢
**预案**：
- 检查网络连接
- 重启服务
- 使用本地数据库和缓存

#### 问题3: 推荐结果不符合预期
**预案**：
- 检查记忆是否被正确检索
- 查看 Agent 轨迹调试
- 手动调整评分权重

#### 问题4: 数据文件损坏
**预案**：
- 定期备份 `data/` 目录
- 准备备用数据文件
- 可以从备份快速恢复

---

## 评委问答准备

### Q1: 你们的记忆机制是怎么工作的？
**A**：
我们采用三层记忆结构：
1. **固定画像**：用户的基本属性（时间、难度基础）
2. **偏好记忆**：用户的偏好（语言、格式、案例偏好）
3. **任务反馈**：对特定书籍的反馈

记忆通过 LLM 压缩为结构化数据，并用向量检索实现语义匹配。

### Q2: 推荐是怎么生成的？
**A**：
采用"规则+LLM"混合模式：
1. 先通过规则评分（主题匹配、难度匹配、时间匹配、偏好匹配）
2. 再用 LLM 生成自然语言解释
3. 这样既保证稳定性，又有灵活性

### Q3: 怎么验证记忆真的被使用了？
**A**：
1. 前后对比页会显示"本次引用的记忆"列表
2. Agent 轨迹会记录所有工具调用
3. 可以展示相同查询下，有无记忆的推荐差异

### Q4: 系统的成本如何控制？
**A**：
1. Embedding 模型本地运行，零 API 成本
2. 书目检索直接本地 SQL 查询
3. 只有记忆压缩和推荐解释调用 LLM
4. 使用缓存减少重复调用

### Q5: 如果用户反馈模糊怎么办？
**A**：
- 记忆压缩会给出置信度分数
- 低置信度记忆在评分中权重较低
- 可以通过 `confidence` 阈值过滤不确定的记忆

---

## 展示重点

### 必须展示的功能
1. ✅ **前后对比**：相同输入，有无记忆的推荐差异
2. ✅ **记忆可视化**：显示引用了哪些记忆
3. ✅ **Agent轨迹**：工具调用链
4. ✅ **评分明细**：每本书的得分构成

### 加分项
1. 实时记忆写入演示
2. 多用户对比
3. 记忆编辑和删除
4. 性能指标展示（响应时间、记忆命中率等）

---

## 最后检查

在展示前：
- [ ] 重启服务（清除临时状态）
- [ ] 准备干净的演示数据
- [ ] 测试所有接口响应
- [ ] 准备降级方案（API 失败时）
- [ ] 确认演示环境可用

---

祝比赛顺利！💪
